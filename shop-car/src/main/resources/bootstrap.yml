spring:
  cloud:
    config:
      uri: http://localhost:9999
      profile: eureka-client,shop-car,datasource,shop-thymeleaf
      name: application
  main:
    allow-bean-definition-overriding: true